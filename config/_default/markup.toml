# -- Markup --
# These settings are required for the theme to function.

[goldmark]
[goldmark.renderer]
  unsafe = true

[highlight]
  noClasses = false
  style = "dracula"  # 원하는 스타일로 변경 가능
  lineNumbersInTable = true
  lineNumbers = true

[tableOfContents]
  startLevel = 1
  endLevel = 3
